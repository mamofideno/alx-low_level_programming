#include <string.h>
#include "main.h"
#include "stdio.h"
/**
 *_strcat - string concat
 *@dest: destination
 *@src: source
 *Return: return character
 */
char * _strcat(char *dest, char *src)
{
return (strcat(dest, src));
}
